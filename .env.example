# Flux 2 Multi-Model API Server Configuration
# Copy this file to .env and customize your settings

# ============================================
# REQUIRED: API Key for authentication
# ============================================
# Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
FLUX_API_KEY=your-secret-api-key-here

# ============================================
# Server Settings
# ============================================
# Port to run the server on
FLUX_PORT=7860

# Host to bind to (0.0.0.0 = all interfaces, 127.0.0.1 = localhost only)
FLUX_HOST=0.0.0.0

# ============================================
# GPU Settings
# ============================================
# GPU ID to use (0 = first GPU, 1 = second GPU, etc.)
FLUX_GPU_ID=0

# ============================================
# Queue Settings
# ============================================
# Maximum number of requests in queue (returns 503 if exceeded)
FLUX_MAX_QUEUE=10

# ============================================
# Output Settings
# ============================================
# Directory for saved images when using response_format="file"
FLUX_OUTPUT_DIR=./generated

# ============================================
# LoRA Settings (Optional)
# ============================================
# Directory containing LoRA files - allows using just filename instead of full path
# Example: FLUX_LORA_DIR=E:\models\loras
FLUX_LORA_DIR=
