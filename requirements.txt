# Flux 2 Multi-Model API Server Dependencies
#
# IMPORTANT: Do NOT install directly with pip install -r requirements.txt
# Instead, run setup.bat which handles PyTorch CUDA and diffusers from git
#
# Manual installation order:
# 1. PyTorch with CUDA: py -3.11 -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# 2. Diffusers from git: py -3.11 -m pip install git+https://github.com/huggingface/diffusers
# 3. Other deps: py -3.11 -m pip install -r requirements.txt

# Hugging Face ecosystem
transformers>=4.40.0
accelerate>=0.27.0
safetensors>=0.4.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# API server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# Image processing
Pillow>=10.0.0
